{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Proxy",
  "main": "app.ts",
  "scripts": {
    "watch": "npx babel --watch src --out-dir public --presets react-app/prod",
    "start": "deno run --allow-net --allow-read --unstable app.js",
    "format": "deno fmt src/ app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2"
  }
}
